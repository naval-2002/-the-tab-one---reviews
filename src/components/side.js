function Side(props) {
  console.log(props);
  // const companies = props.datahandler;

  return (
    <div>
      {/* {props.map((val) => {
        return (
          <button
            onClick={() => {
              props.datahandler(val);
            }}
          >
            {val}
          </button>
        );
      })} */}
    </div>
  );
}
export default Side;
